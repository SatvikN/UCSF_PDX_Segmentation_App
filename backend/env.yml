name: pdx-backend
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - fastapi>=0.110
      - uvicorn[standard]>=0.29
      - python-multipart>=0.0.9
      - pydicom>=2.4
      - numpy>=1.24
      - pillow>=10.0
      - h5py>=3.9
      - scikit-image>=0.22
      # Select the right TF package manually if needed:
      # On Apple Silicon (arm64): tensorflow-macos>=2.13
      # Otherwise: tensorflow>=2.13

